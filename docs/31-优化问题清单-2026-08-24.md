# 优化问题清单

本清单从优化前基线开始维护。每个条目完成时必须同时补充代码变更、测试、证据和回滚说明；不同门禁不合并成一个无法定位的“大重构”。

## P0：阻断学生试运行

- [x] `CONTRACT-001` 建立独立的 Phase 1 产品目标契约、状态晋级门禁和公开数据防过拟合策略（见 `contracts/phase1_product_target.json`、`docs/37-Phase1-目标模式与公开数据证据策略.md`）。

- [x] `SEC-001` 生产 SQLAlchemy engine 改为配置控制，默认 `DB_ECHO=false`，并有回归测试。
- [x] `SEC-002` CORS 改为 `BACKEND_CORS_ORIGINS` 白名单，移除通配来源并收紧方法/请求头。
- [x] `SEC-003a` `/mail/test` 默认不注册；仅显式 `ENABLE_MAIL_TEST=true` 的开发/staging 环境启用。
- [x] `SEC-003b` 验证码发送按邮箱/IP限流，验证码使用 `secrets` 生成并支持过期、失败次数上限和一次性消费；历史记录保留用于日限额统计。
- [x] `SEC-004` 教师邀请码改为 `TEACHER_INVITE_CODE` 配置；未配置时教师邀请码注册明确禁用，错误邀请码不再降级为学生。
- [x] `DB-001` 业务持久化数据库固定为 MySQL 8.0.46；已完成完整 Alembic 升级、回退、再次升级、备份恢复和并发连接演练（见 `docs/33-业务数据库迁移演练记录-2026-08-24.md`）。当前判题层 Docker/native MySQL 8.0.46 仍属于独立执行器，不属于业务数据库迁移；此前 8.4 仅为历史 runner 版本。
- [x] `P1-001` 明确方言、数据库版本和支持的 SQL 特性契约（`docs/34-Phase1-支持契约与验收术语.md`、`contracts/phase1_product_target.json`、`contracts/phase1_current_implementation.json`）。
- [ ] `P1-002` 分析并修复 v16 的 22 个确定性 mismatch。
- [x] `P1-002a` 修复并公开回归已定位的 witness 可达性/兼容性缺口：字符串过滤下的 COUNT/MIN/MAX/DISTINCT，以及 generic 双引号 schema 列名；随后补上简单非相关 `IN`/`EXISTS` 的部分键 membership witness，并对矛盾 NULL 谓词、单调递归 UNION 去重形状增加保守门禁；当前 mutation 会避开 `PRIMARY KEY/NOT NULL` 列，避免生成语义等价的 `COUNT(*) → COUNT(column)` 伪反例，并会避开单列 `PRIMARY KEY/UNIQUE` 直接投影上的 DISTINCT 伪反例。Gold Oracle 随后补齐 Unicode/数字开头 schema 标识符的 literal seeding、简单复合 WHERE 的边界行对齐和 SUM/AVG 等变更的数值分离 witness。2026-08-26 全量 public replay 读取 mutation/equivalence 各 `1,051` 行，15/15 required families 全覆盖；Gold `2,102/2,102` 均已分类（`1,025 EQUIVALENT / 1,018 NOT_EQUIVALENT / 45 ENGINE_GAP / 14 INPUT_GAP / 0 UNDECIDED`），structure/atomic obligation coverage 均为 `1.0`；production chain 为 `1,025 PASS / 26 EXCLUDED / 0 FAIL`。其中 MySQL 8.0.46 Linux `lower_case_table_names=0` 的真实 profile 已验证，schema/query 名称不一致继续明确归入 `INPUT_GAP`。
- [x] `P1-DATA-001` 收紧 Spider source collector：只有携带 `schema_catalog` 或 `authoritative_source_catalog` provenance 的本地 Spider 行才可绕过官方 `tables.json`；旧的 query-text 推断 schema（会把 `T1/T2` 误当列）明确拒绝，避免污染公开 replay 证据。回归覆盖接受权威 schema 与拒绝推断 schema 两条路径。
- [x] `P1-DATA-002` 在线公开 SQL smoke builder 在候选 mutation/parse 和逐案 evaluator 阶段增加 POSIX 硬超时；解析异常或超时样本 fail-closed 跳过/记录为 `RESOURCE_LIMIT + UNDECIDED`，不阻塞公开数据构建。公开 v2 smoke（seed `20260825`、50 cases）为 `38 PASS / 11 KNOWN_GAP / 1 ENGINE_GAP`，无错误判题；回归覆盖超时恢复和隔离评估。
- [ ] `P1-003` 分析并修复 v16 的 7 个 parser/input generation gap。公开 target v3 mutation layer 和 2026-08-25 新公开 snapshot 均无生成缺口；新 hidden v20、v22、v23 也达到 `generation_failures=0`，但旧 v16 family 身份不能读取，且 v23 的 determinate mismatch 仍未闭合，故仍不把“旧 7 个”宣称逐一修复。
- [ ] `P1-004` 用新的 hidden 快照重新冻结，要求 generation failure=0、determinate mismatch=0、重复运行稳定且 `acceptance.pass=true`。v20 达到 `generation_failures=0`、`repeat_run_stable=true` 但有 5 个 mismatch；公开修复后标准配置 v21 仍 generation-complete/stable，mismatch 降至 4 个；全新 v22、v23 snapshots 均达到 `generation_failures=0`、`repeat_run_stable=true`，但各有 11 个 mismatch；最新更大 v24 snapshot 保持 `repeat_run_stable=true`，但有 `54` 个 generation failures（53 parse、1 render）和 `42` 个 determinate mismatch，`acceptance.pass=false`。hidden mismatch/失败仅记录 digest，不作为定向优化输入。
- [ ] `WORKER-001` 将 Phase 1 从不可强杀的 `asyncio.to_thread` 迁移到可强制终止的进程/Worker，并增加队列、CPU、内存、超时和重建门禁。按请求 spawn child、私有 process group、超时时 descendant kill、CPU/地址空间限制、队列上限和 child crash 后重建已有公开回归；常驻 worker 池、跨实例队列和真实 OOM 演练仍未完成。

## P1：真实学生可试运行

- [ ] `QM-001` 盘点所有 active 题目，确保每题至少一个 PRIMARY 原子技能。
- [ ] `QM-002` 清理 broad/atomic 混用，旧题映射必须人工声明或审核，不从 `correct_sql` 猜测。
- [ ] `QM-003` CI 禁止新增没有有效 Q-matrix 的 active 题目。
- [ ] `ATTEMPT-001` 建立服务端 AttemptContext，记录提示、答案揭示、执行次数、语法失败、abandon/resume 等事件。
- [ ] `ATTEMPT-002` `answer_revealed` 改为服务端事实；看过答案后的正确提交不得产生正向 BKT。
- [ ] `FE-001` 实现登录、题目、SQL 编辑、提交、反馈和历史的最小纵向页面。
- [ ] `FE-002` 验证 attempt_id 重试复用、重复响应不重复播放反馈、409/422/503/降级状态展示。
- [ ] `E2E-001` 在临时 MySQL 上完成真实前后端 E2E，而非仅 API 单元测试。

## P2：完整自适应教学与运营

- [ ] `ADAPT-001` 将 challenge_readiness 接入难度区间、技能候选、先修、去重和历史过滤。
- [ ] `ADAPT-002` 保存下一题分配决策审计。
- [ ] `OPS-001` 增加 Phase 1 超时率、UNDECIDED 率、fallback 率、BKT 降级率和审计缺失监控。
- [ ] `OPS-002` 增加 chat-submission 关联、教学反馈补偿/对账机制。
- [ ] `OPS-003` 制定 SQL、聊天、反馈和学习状态的保留、导出、删除、匿名化策略。
- [ ] `CAL-001` 使用真实轨迹按用户和时间切分校准 BKT，并报告 Brier、log-loss 和 calibration curve。
- [ ] `CAL-002` 对 L1-L4 支架做离线回放和 A/B 验证。

## 明确暂不做

- [x] 不让 LLM 脱离确定性证据决定 verdict、创造错因或支架等级；允许它在已有 strong candidate 集合内有限排序，并由下游 provenance/gate 再次校验 target。
- [ ] 不为“算法名称”重新引入 A*。
- [ ] 不在真实轨迹和数据契约完成前调整 BKT 参数或宣称教学效果。
- [ ] 不为扩大覆盖率而把 Phase 2 的 20 条规则包装成任意 SQL 全覆盖。
