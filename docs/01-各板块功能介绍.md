# SQL 智能教学系统 — 各板块功能介绍

本文档按前后端模块列出各板块功能，便于新人快速了解系统能力与入口。Phase 3～6 的当前实现以《27-Phase3-技能观测契约与Q-matrix基础》《28-Phase3-v1-因果调度与原始BKT实现》和《29-Phase4-6-v1-教学支架反馈与事务闭环》为准。

---

## 一、后端（FastAPI）

### 1. 认证与用户（`/auth`）

| 功能 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 发送邮箱验证码 | GET | `/auth/code?email=xxx` | 注册/找回时发送 6 位验证码到邮箱 |
| 用户注册 | POST | `/auth/register` | 校验验证码后创建账号，可选教师邀请码 |
| 用户登录 | POST | `/auth/login` | 返回 access_token / refresh_token |
| 刷新 Token | POST | `/auth/refresh` | 用 refresh_token 换新 access_token |
| 获取当前用户信息 | GET | `/auth/profile` | 需 Bearer Token，返回用户基本信息 |
| 注销账户 | POST | `/auth/delete-account` | 校验邮箱与密码后永久删除账号 |

**相关代码**：`routers/auth.py`、`repository/user_repo.py`、`core/auth.py`。

---

### 2. 题目管理（`/questions`）

| 功能 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取题目列表 | GET | `/questions/?skip=0&limit=100` | 分页，含动态难度 |
| 获取题目详情 | GET | `/questions/{question_id}` | 单题详情，无 schema_preview 时自动生成 |
| 创建题目 | POST | `/questions/` | 需教师权限；难度/结果列名可留空由系统推断 |
| 更新题目 | PUT | `/questions/{question_id}` | 需教师权限 |
| 删除题目 | DELETE | `/questions/{question_id}` | 需教师权限，级联删除相关提交 |
| 获取知识点列表 | GET | `/questions/knowledge-points` | 需教师权限，入门/进阶/精通多语言 |
| 按知识点 AI 生成题目 | POST | `/questions/generate-by-ai` | 需教师权限，1～5 道 |
| 生成题目多语言题面 | POST | `/questions/{id}/generate-i18n` | 已登录即可，为题目生成英/繁体 |
| 生成表结构预览 | POST | `/questions/{id}/generate-schema-preview` | 需教师权限，AI 生成 schema_preview |
| 推断结果列名 | POST | `/questions/infer-output-columns` | 需教师权限，从标准答案 SQL 解析列名 |
| 提交难度反馈 | POST | `/questions/{id}/difficulty-feedback` | 学生正确完成后可对题目难度打分 1～10 |

**相关代码**：`routers/question.py`、`repository/question_repo.py`、`core/sql_knowledge_points.py`、`core/ai_question_generator.py`、`core/sql_parser.py`、`core/difficulty_service.py`。

---

### 3. 基于闭环控制理论的 AI 判题与对话（`/ai`）

| 功能 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 获取学生能力雷达 | GET | `/ai/mastery-radar` | 返回学生当前各知识点的 BKT($L_t$) 后验概率，供前端呈现雷达图 |
| 检查 SQL 判题反馈 | POST | `/ai/check-sql` | 权威执行判题、Phase 2 有界诊断、Q-matrix/原子 rule 观测、原始 BKT 与确定性分级教学反馈；不向学生暴露诊断包 |
| 获取对话历史 | GET | `/ai/chat/messages?question_id=xx&limit=50` | 当前用户在该题下的聊天记录 |
| 发送对话消息 | POST | `/ai/chat` | 依据当前上下文与 AI 教师进行计算思维与苏格拉底多轮追问 |
| 清除对话历史 | DELETE | `/ai/chat/messages?question_id=xx` | 清除当前用户在该题下的所有消息 |
| SQL 提示（仅测试） | POST | `/ai/sql-hint` | 仅调用 AI 提示，无闭环逻辑 |

**当前核心代码**：
- `core/parseval_data_generator.py`、`core/error_diagnosis.py`：Phase 1 有界判定结果与 Phase 2 规则化诊断。
- `core/phase3_skill_catalog.py`、`core/phase3_observation.py`：`rule_id → atomic skill` 与 Q-matrix 观测门控。
- `core/causal_priority_scheduler.py`、`core/support_policy.py`：受约束目标排序、support_need 与独立的 challenge_index。
- `core/phase3_bkt.py`、`repository/phase3_learning_repo.py`：标准四参数 BKT 原始 posterior/next_prior 与事件幂等落库。

---

### 4. 其他

| 功能 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 健康检查 | GET | `/` | 返回运行中提示 |
| 邮件测试 | GET | `/mail/test?email=xxx` | 仅开发/测试环境显式启用，生产默认禁用 |

**相关代码**：`main.py`、`dependencies.py`、`core/mail.py`。

---

## 二、前端（uni-app Vue3）

### 1. 登录页（`pages/login/index`）

- **功能**：登录、注册、注销账户；顶部语言切换（简体/English/繁體）。
- **接口**：`/auth/code`、`/auth/register`、`/auth/login`、`/auth/delete-account`。
- **说明**：验证码倒计时、Token 存本地、登录后跳转首页或教师端（按角色）。

---

### 2. 学生练习页（`pages/index/index`）

- **功能**：选题、查看题目与表结构预览、编写 SQL、提交动态判题、查看教学反馈、与 AI 教师对话。
- **接口**：`/auth/profile`、`/questions/`、`/questions/{id}`、`/ai/mastery-radar`、 `/ai/check-sql`、`/ai/chat/messages`、`/ai/chat`、`/ai/chat/messages`（DELETE）。
- **说明**：题目/知识点多语言；提交结果只有在权威 Q-matrix 或 Phase 2 强证据满足门控时才产生对应学习观测；支架等级由可审计行为代理和 BKT 状态决定，不宣称测量心理疲劳。

---

### 3. 教师端（`pages/teacher/index`）

- **功能**：题目列表、新建/编辑/删除题目、按知识点 AI 生成题目、为当前题目补全多语言、语言切换。
- **接口**：`/auth/profile`、`/questions/`、`/questions/{id}`、`/questions/`（POST/PUT/DELETE）、`/questions/knowledge-points`、`/questions/generate-by-ai`、`/questions/{id}/generate-i18n`。

---

## 三、数据与配置要点

- **数据库**：MySQL（主库），判题时按题目 `schema_preview` 在会话中建表/插数据再执行 SQL。
- **认证**：JWT access_token + refresh_token，请求头 `Authorization: Bearer <token>`。
- **教学组件数据流**：学生答题 -> Phase 1 权威判定 -> Phase 2 有界规则诊断 -> Q-matrix/rule-to-skill 门控 -> 原始 BKT 与 `ConstrainedPriorityScheduler` -> Phase 4/5 确定性支架反馈。
- **判题沙盒**：禁止所有写操作和管理指令；仅允许 SELECT 以及部分合法聚合连表；采用严格的数据清理与大小写抹平。

---

*文档版本：控制理论闭环架构 V2 最终版。*
