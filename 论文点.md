# Phase 1 系统能力的形式化定义、支持边界与实证证据


## 摘要式说明

本文实现了一套面向参考 SQL 与待分析 SQL 的分层差异诊断系统。系统能够将 SQL 解析为结构化中间表示，提取原子级 AST 差异，将差异转换为语义验证义务，并通过有限数据库构造、执行结果比较、候选修复验证和知识点归因形成样本级诊断证据。

本文严格区分结构支持、受界执行支持、变异—修复支持和端到端链路完成证据，不将语法可解析性、mutation 代码路径存在或有限测试一致性直接解释为完整诊断能力。所有能力声明均以具体查询对、共享 schema、声明方言、资源配置和实际证据为参数，而不是对某一 SQL 类别作无条件的布尔声明。

---

# 1. 研究对象与问题定义

## 1.1 输入对象

Phase 1 处理的基本对象定义为：

$$
x=\langle q_r,q_s,\Sigma,d\rangle
$$

其中：

- $q_r$ 为参考 SQL；
- $q_s$ 为待分析或学生 SQL；
- $\Sigma$ 为两条 SQL 共享的数据库 schema；
- $d$ 为输入声明的 SQL 方言。

系统的目标不是对任意 SQL 给出形式化等价证明，而是在给定 schema、声明方言和资源上限的条件下：

1. 提取两条查询之间的结构差异；
2. 将结构差异转换为可验证的语义义务；
3. 构造可能暴露差异的有限数据库实例；
4. 比较两条查询在这些数据库实例上的执行结果；
5. 验证候选修复是否消除目标差异；
6. 将得到验证的差异映射到错误类型或知识点。

因此，本文所称的“支持”是关于具体输入、具体 SQL 特性和具体执行配置的关系，而不是某一 SQL 类别上的无条件布尔属性。

## 1.2 系统处理链

系统处理流程为：

```text
解析
→ SQLStructureIR
→ ASTDiff
→ obligation
→ witness
→ 执行比较
→ mutation/repair 验证
→ attribution
```

形式化写为：

$$
\langle q_r,q_s,\Sigma,d\rangle
\rightarrow
\langle I_r,I_s\rangle
\rightarrow
\Delta
\rightarrow
\Omega
\rightarrow
\mathcal W_B
\rightarrow
v
\rightarrow
E
\rightarrow
KP.
$$

这里：

- $I_r,I_s$ 是两条 SQL 的结构化中间表示；
- $\Delta$ 是 atomic AST difference 集合；
- $\Omega$ 是 semantic obligation 集合；
- $\mathcal W_B$ 是受界配置下构造的有限数据库集合；
- $v$ 是执行 Oracle 的判定结果；
- $E$ 是验证和修复证据；
- $KP$ 是归因得到的知识点集合。

## 1.3 系统偏函数

系统主要阶段可表示为以下偏函数：

$$
\begin{aligned}
\mathsf{Parse}_d &: Q \rightharpoonup I,\\
\mathsf{Diff} &: I\times I \rightarrow 2^\Delta,\\
\mathsf{Obl} &: \Delta\times\Sigma \rightharpoonup 2^\Omega,\\
\mathsf{World} &: 2^\Omega\times\Sigma\times B
                  \rightharpoonup 2^{DB},\\
\mathsf{Exec}_d &: Q\times DB \rightharpoonup Result,\\
\mathsf{Validate} &: \Delta\times\Omega\times DB
                     \rightarrow Evidence,\\
\mathsf{Mutate}_\mu &: Q\times\Sigma\rightharpoonup Q,\\
\mathsf{Repair} &: Q\times\Delta\rightharpoonup Q,\\
\mathsf{Attribute} &: \Delta\times Evidence
                      \rightharpoonup 2^{KP}.
\end{aligned}
$$

符号 $\rightharpoonup$ 表示偏函数。任一阶段可能因为输入、作用域、方言、执行器或资源条件而无定义，因此任一步失败都不能被解释为“查询等价”或“系统判断正确”。

---

# 2. 分层支持定义

令：

$$
D(x)=\mathsf{Diff}
\bigl(\mathsf{Parse}_d(q_r),\mathsf{Parse}_d(q_s)\bigr)
$$

表示输入 $x$ 的 atomic diff 集合。

支持等级必须针对具体输入或具体 mutation 实例报告。例如：

$$
Support_k(x;B)
$$

表示输入 $x$ 在受界配置 $B$ 下达到等级 $S_k$。

## 2.1 S1：结构支持

$$
\begin{aligned}
S_1(x)\iff\;&
\mathsf{Parse}_d(q_r)\downarrow
\land
\mathsf{Parse}_d(q_s)\downarrow\\
&\land D(x)\downarrow
\land
\mathsf{WellFormed}(D(x)).
\end{aligned}
$$

符号 $\downarrow$ 表示偏函数有定义。

S1 只说明两条查询能够解析、构建结构化 IR，并产生具有明确位置和类型的 ASTDiff。它不蕴含可执行性：

$$
S_1(x)\not\Rightarrow S_2(x;B).
$$

## 2.2 S2：受界执行支持

令 Oracle 的输出空间为：

$$
\mathcal V=
\{
\mathsf{NOT\_EQUIVALENT},
\mathsf{EQUIVALENT},
\mathsf{UNDECIDED},
\mathsf{ENGINE\_GAP},
\mathsf{INPUT\_GAP}
\}.
$$

定义：

$$
S_2(x;B)
\iff
\mathsf{Oracle}(x;B)
\in
\{
\mathsf{NOT\_EQUIVALENT},
\mathsf{EQUIVALENT},
\mathsf{UNDECIDED}
\}.
$$

即只有当系统完成了两条查询的受界执行和结果比较，才称输入达到 S2。`ENGINE_GAP` 和 `INPUT_GAP` 均不构成 S2 证据。

## 2.3 S3：变异—修复支持

设 $\rho_\mu$ 是 mutation family $\mu$ 对一个合格 AST 节点实施的一次局部重写。令：

$$
q_\mu=
\mathsf{Render}
\left(
\rho_\mu(\mathsf{Parse}_d(q))
\right).
$$

当前实现层面的 mutation 可生成性定义为：

$$
\begin{aligned}
\mathsf{Generated}(q,\mu)\iff\;&
q_\mu\neq q\\
&\land \mathsf{Parse}_d(q_\mu)\downarrow.
\end{aligned}
$$

但生成成功不足以构成 S3。本文将 S3 定义为：

$$
\begin{aligned}
S_3(q,\mu;\Sigma,d,B)\iff\;&
\mathsf{Generated}(q,\mu)\\
&\land
\mathsf{Oracle}
(\langle q,q_\mu,\Sigma,d\rangle;B)
=\mathsf{NOT\_EQUIVALENT}\\
&\land
\mathsf{RepairVerified}(q_\mu,q).
\end{aligned}
$$

因此：

$$
\mathsf{Generated}(q,\mu)
\not\Rightarrow
S_3(q,\mu;\Sigma,d,B).
$$

这一区分防止把 mutation manifest 的生成覆盖率错误解释成 S3 覆盖率。

## 2.4 当前 S4-audit

设：

- $D$ 为 atomic diff ID 集合；
- $O$ 为已绑定 obligation 的 diff ID 集合；
- $V$ 为已激活 semantic validator 的 diff ID 集合；
- $R$ 为得到 repair 证据的 diff ID 集合；
- $K_D$ 为 diff 对应的知识点集合；
- $K_A$ 为 attribution 输出的知识点集合。

当前 production-chain audit 的实际条件可形式化为：

$$
S_4^{audit}(x)
\iff
D\subseteq O
\land
D\subseteq V
\land
D\cap R\neq\varnothing
\land
K_D\cap K_A\neq\varnothing.
$$

这表示：

- 所有 atomic diff 都已产生 obligation；
- 所有 atomic diff 都已激活语义 validator；
- 至少一个 atomic diff 获得 repair 证据；
- attribution 与目标 diff 至少存在一个知识点交集。

该条件弱于严格的逐差异闭环。严格 S4 应定义为：

$$
S_4^{strict}(x)
\iff
\forall\delta\in D,
\begin{cases}
\delta\in O,\\
\delta\in V,\\
\delta\in R,\\
\mathsf{AttrBound}(\delta),\\
\mathsf{EvidenceReplayable}(\delta).
\end{cases}
$$

因此：

$$
S_4^{audit}(x)
\not\Rightarrow
S_4^{strict}(x).
$$

在当前论文中，宜使用“样本级端到端链路完成证据”或“S4-audit PASS”，而不是直接声明严格 S4。

---

# 3. 受界 Gold Oracle 的操作语义

## 3.1 受界配置

定义受界执行配置：

$$
B=
\langle
Seeds,Scales,R_{\max},O_{\max},C_{\max}
\rangle.
$$

其中：

- $Seeds$ 是随机种子集合；
- $Scales$ 是数据规模集合；
- $R_{\max}$ 是单表行数上限；
- $O_{\max}$ 是结果行数上限；
- $C_{\max}$ 是执行资源上限。

v15/v16 审计产物采用：

$$
\begin{aligned}
Seeds &= \{0,\ldots,9\},\\
Scales &= \{4,8,16\},\\
R_{\max} &= 32,\\
O_{\max} &= 256,\\
C_{\max}^{SQLite} &= 250000
\quad\text{VM steps}.
\end{aligned}
$$

这些数值是 v15/v16 审计运行时的显式配置，不是 Gold Oracle 函数在所有调用场景下不可修改的固定默认值。

## 3.2 有限数据库集合

数据生成器依据 schema 构造有限数据库集合：

$$
\mathcal W_B(\Sigma)
=
\{D_1,D_2,\ldots,D_n\}.
$$

对每个数据库实例 $D_i$，Oracle 比较两条查询的规范化可观察结果：

$$
\llbracket q\rrbracket_{d,D}
=
\mathsf{Normalize}
\left(
\mathsf{Exec}_d(q,D)
\right).
$$

若查询被实现判定为有序查询，则比较规范化后的结果序列；否则按重复项敏感的多重集语义比较。结果列数量和行值参与比较，输出列名不作为等价条件。

当前实现的有序性判定可近似表示为：

$$
\mathsf{Ordered}_{impl}(q_r,q_s)
=
\mathsf{ContainsOrderBy}(q_r)
\lor
\mathsf{ContainsOrderBy}(q_s).
$$

因此：

$$
a\equiv_q b=
\begin{cases}
a=b,
& \mathsf{Ordered}_{impl}(q_r,q_s)=1,\\
\mathsf{Bag}(a)=\mathsf{Bag}(b),
& \text{otherwise}.
\end{cases}
$$

该实现是文本级近似，而不是严格的顶层 AST `ORDER BY` 判定，可能受到嵌套 `ORDER BY` 影响，应作为 Oracle 的内部有效性边界公开说明。

## 3.3 Oracle 判定

受界不等价定义为：

$$
NE_B(q_r,q_s)
\iff
\exists D\in\mathcal W_B(\Sigma):
\llbracket q_r\rrbracket_{d,D}
\not\equiv_q
\llbracket q_s\rrbracket_{d,D}.
$$

因此，一个可重放的反例数据库足以证明两条查询在相应执行器语义下不等价。

相反：

$$
\forall D\in\mathcal W_B(\Sigma):
\llbracket q_r\rrbracket_{d,D}
\equiv_q
\llbracket q_s\rrbracket_{d,D}
$$

只说明在已经探索的有限世界中没有发现反例，不能推出普遍关系等价。

Oracle 的完整判定函数为：

$$
\mathsf{Oracle}(x;B)=
\begin{cases}
\mathsf{INPUT\_GAP},
& \neg\mathsf{Replayable}(x),\\[2mm]
\mathsf{ENGINE\_GAP},
& \neg\mathsf{EngineAvailable}(d)
  \lor \mathsf{ExecutionFailure},\\[2mm]
\mathsf{NOT\_EQUIVALENT},
& \exists D\in\mathcal W_B(\Sigma):
  \llbracket q_r\rrbracket_{d,D}
  \not\equiv_q
  \llbracket q_s\rrbracket_{d,D},\\[2mm]
\mathsf{EQUIVALENT},
& \mathsf{TrustedEqControl}(x)
  \land
  \forall D\in\mathcal W_B(\Sigma):
  \llbracket q_r\rrbracket_{d,D}
  \equiv_q
  \llbracket q_s\rrbracket_{d,D},\\[2mm]
\mathsf{UNDECIDED},
& \text{otherwise}.
\end{cases}
$$

所以：

$$
\mathsf{NOT\_EQUIVALENT}
\Rightarrow
q_r\not\equiv q_s
$$

成立的前提是执行器语义可信且 witness 可以重放。

但不能推出：

$$
\mathsf{UNDECIDED}
\Rightarrow
q_r\equiv q_s.
$$

系统中的 `EQUIVALENT` 是可信标签支持下的有限世界一致，不是关系代数意义上的普遍等价证明。

## 3.4 方言执行语义

对于声明为 `generic`、`standard` 或 `sqlite` 的查询，当前 Oracle 使用 SQLite 执行路径。因此这里的 `standard` 应理解为：

> 可以由当前解析器处理并能够在 SQLite 执行路径上运行的标准风格 SQL 子集。

它不表示系统实现了完整 SQL Standard 的执行语义。

MySQL、PostgreSQL、T-SQL 和 Oracle 已实现可选 native adapter。只有在相应执行器已经配置且可达时，系统才能产生该方言的原生执行证据；否则返回 `ENGINE_GAP`，不会静默转用 SQLite。

---

# 4. Phase 1 保守支持片段的上下文无关语法

## 4.1 文法与能力声明的关系

上下文无关语法只能定义系统声明的语法输入域，不能表达：

- 表名和列名是否能在 schema 中解析；
- 相关子查询引用是否满足作用域规则；
- 某个方言的 native runner 是否可用；
- 数据生成器是否能构造区分性 witness；
- 查询是否会超过资源上限；
- mutation 是否能得到修复验证；
- attribution 是否与 atomic diff 建立一致绑定。

因此，本文采用“CFG + 语义侧条件 + 证据谓词”的三层定义。

令 $G_{P1}$ 为下面的上下文无关语法。查询满足：

$$
q\in L(G_{P1})
$$

只表示该查询具有进入 Phase 1 分析流程的语法资格，不直接表示该查询达到 S2、S3 或 S4-audit。

实际支持域定义为：

$$
\mathcal D_k(B)=
\left\{
x\;\middle|\;
q_r,q_s\in L(G_{P1})
\land
\Phi(x)
\land
S_k(x;B)
\right\}.
$$

其中 $\Phi(x)$ 是 schema、作用域、方言和资源侧条件。

## 4.2 紧凑 EBNF 表示

为了适合作为论文中的 Grammar Figure，下面采用 EBNF 简写：

- `[X]` 表示可选；
- `{X}` 表示零次或多次；
- `X | Y` 表示候选产生式；
- `ε` 表示空串。

EBNF 与普通 CFG 具有相同表达能力，可机械展开为严格 BNF。

```text
Q   ::= [ W ] E [ O ] [ L ]                         query
W   ::= WITH [ RECURSIVE ] C { "," C }              CTE clause
C   ::= I [ "(" IL ")" ] AS "(" Q ")"               CTE definition

E   ::= S { U S }                                    query expression
U   ::= UNION [ ALL ]
      | INTERSECT
      | EXCEPT                                       set operator

S   ::= SELECT [ DISTINCT ] X
        FROM R
        [ WHERE P ]
        [ GROUP BY EL ]
        [ HAVING P ]                                 select query

X   ::= XI { "," XI }                                select list
XI  ::= "*"
      | I "." "*"
      | e [ A ]                                      select item
A   ::= [ AS ] I                                     alias

R   ::= F
      | R "," F
      | R JT JOIN F ON P                             table relation
F   ::= T [ A ]
      | "(" Q ")" A                                  table/subquery
JT  ::= ε
      | INNER
      | LEFT [ OUTER ]
      | RIGHT [ OUTER ]
      | FULL [ OUTER ]                               join type

P   ::= P OR PA
      | PA                                           disjunction
PA  ::= PA AND PN
      | PN                                           conjunction
PN  ::= NOT PN
      | "(" P ")"
      | e CP e
      | e IS NULL
      | e IS NOT NULL
      | e [ NOT ] IN "(" IQ ")"
      | [ NOT ] EXISTS "(" Q ")"
      | e [ NOT ] BETWEEN e AND e
      | e [ NOT ] LIKE e                             predicate

IQ  ::= EL
      | Q                                            list/subquery
CP  ::= "=" | "<>" | "!="
      | "<" | "<=" | ">" | ">="                     comparison

e   ::= e "+" t
      | e "-" t
      | t                                            additive expression
t   ::= t "*" f
      | t "/" f
      | f                                            multiplicative expr.
f   ::= V
      | I
      | I "." I
      | "(" e ")"
      | "(" Q ")"
      | AG
      | CA
      | WF
      | FN                                           scalar expression

V   ::= NUM | STR | NULL | TRUE | FALSE              literal
FN  ::= BI "(" [ EL ] ")"                            built-in function

AG  ::= MIN "(" [ DISTINCT ] e ")"
      | MAX "(" [ DISTINCT ] e ")"
      | SUM "(" [ DISTINCT ] e ")"
      | AVG "(" [ DISTINCT ] e ")"
      | COUNT "(" "*" ")"
      | COUNT "(" [ DISTINCT ] e ")"                 aggregate

CA  ::= CASE e SW { SW } [ CE ] END
      | CASE PW { PW } [ CE ] END                    CASE expression
SW  ::= WHEN e THEN e                                simple CASE branch
PW  ::= WHEN P THEN e                                searched CASE branch
CE  ::= ELSE e                                       ELSE branch

WF  ::= ROW_NUMBER "(" ")" OVER "(" WS ")"
      | RANK "(" ")" OVER "(" WS ")"
      | AG OVER "(" WS ")"                           window function
WS  ::= [ PARTITION BY EL ]
        [ ORDER BY OL ]                              supported window spec.

O   ::= ORDER BY OL                                  result ordering
OL  ::= OI { "," OI }
OI  ::= e [ ASC | DESC ]
        [ NULLS FIRST | NULLS LAST ]                 order item

L   ::= LIMIT N [ OFFSET N ]                         pagination

EL  ::= e { "," e }                                  expression list
IL  ::= I { "," I }                                  identifier list

I   ::= identifier
T   ::= table_identifier
BI  ::= supported_builtin_identifier
N   ::= integer_literal
NUM ::= numeric_literal
STR ::= string_literal
```

## 4.3 严格 CFG 展开

若论文或审稿要求完全不使用 EBNF，可以采用以下机械展开规则：

$$
\begin{aligned}
[X]
&\rightsquigarrow
X_{opt}\rightarrow X\mid\epsilon,\\
\{X\}
&\rightsquigarrow
X_{list}\rightarrow X\,X_{list}\mid\epsilon,\\
X\{,X\}
&\rightsquigarrow
X_{list}\rightarrow X\mid X, X_{list}.
\end{aligned}
$$

例如：

```text
<WOpt>       ::= <W> | ε
<CteTail>    ::= "," <C> <CteTail> | ε
<Distinct>   ::= DISTINCT | ε
<WhereOpt>   ::= WHERE <P> | ε
<GroupOpt>   ::= GROUP BY <EL> | ε
<HavingOpt>  ::= HAVING <P> | ε
<OffsetOpt>  ::= OFFSET <N> | ε
<Direction>  ::= ASC | DESC | ε
<NullOrder>  ::= NULLS FIRST | NULLS LAST | ε
```

由这些展开规则得到标准上下文无关语法：

$$
G_{P1}=\langle N,\Sigma_t,P,Q\rangle.
$$

其中：

- $Q$ 是开始符号；
- $N$ 是非终结符集合；
- $\Sigma_t$ 是 SQL 关键字、标识符和字面量构成的终结符集合；
- $P$ 是产生式集合。

## 4.4 Figure caption

英文图注：

> **Figure X: Grammar of the conservatively claimed Phase 1 SQL fragment.** Square brackets denote optional constructs, braces denote repetition, and vertical bars denote alternatives. Membership in the grammar defines only the syntactic eligibility of an input query. Schema resolution, dialect executability, bounded-world construction, mutation applicability, repair validation, and attribution are governed by separate semantic predicates and must not be inferred from grammatical membership alone.

中文图注：

> **图 X：Phase 1 保守声明 SQL 片段的上下文无关语法。** 方括号表示可选结构，花括号表示重复结构，竖线表示候选产生式。查询属于该文法只表示其具有进入系统分析流程的语法资格；schema 解析、方言可执行性、有限世界构造、mutation 可适用性、修复验证和错误归因由独立语义谓词决定，不能由文法成员关系直接推出。

---

# 5. CFG 之外的语义侧条件

## 5.1 Schema 和名称解析

令 $\Phi_\Sigma(x)$ 表示：

- 所有表和列均能在 $\Sigma$ 中解析；
- 非相关列引用在当前查询块内唯一可绑定；
- 相关子查询引用能够绑定到可见外层作用域；
- mutation 替换出的 JOIN 列必须是 schema 中存在的合法列；
- 查询及 schema 能被 Oracle 重放。

## 5.2 聚合合法性

令 $\Phi_{agg}(x)$ 表示：

- 非聚合投影满足目标方言的 GROUP BY 规则；
- HAVING 中的列引用和聚合引用能够解析；
- 当前 HAVING-threshold mutation 只适用于整数形式的数值字面量；
- GROUP BY deletion mutation 要求原查询至少含有两个分组表达式。

## 5.3 递归 CTE

令 $\Phi_{rec}(x)$ 表示：

- CTE 具有可识别的 anchor member 和 recursive member；
- recursive member 能够绑定到当前 CTE；
- 递归执行在资源上限内终止；
- 当前 mutation 主要适用于整数加减步进或数值终止条件。

CFG 无法表达“递归成员必须引用当前 CTE”，因此该约束必须通过名称解析、属性文法或后续语义检查实现。

## 5.4 方言与函数

令 $\Phi_d(x)$ 表示：

- generic/standard 查询可以由 SQLite-compatible 路径执行；
- vendor 查询具有可达的 native runner；
- `BI` 属于相应方言允许执行的内建函数集合；
- 系统不依赖未知用户定义函数的跨方言一致语义。

## 5.5 资源约束

令 $\Phi_B(x)$ 表示执行过程不超过：

- 单表行数上限；
- 最大结果行数；
- SQLite VM 步数限制；
- native runner 的连接和执行超时；
- 系统允许的递归、CPU 和内存边界。

最终语义侧条件为：

$$
\Phi(x)=
\Phi_\Sigma(x)
\land
\Phi_{agg}(x)
\land
\Phi_{rec}(x)
\land
\Phi_d(x)
\land
\Phi_B(x).
$$

---

# 6. Mutation 重写系统

## 6.1 重写系统定义

当前 mutation 层可以定义为带标签的局部重写系统：

$$
\mathcal R_\mu=
\{
\rho_{cmp},
\rho_{logic},
\rho_{where},
\rho_{join},
\rho_{group},
\rho_{agg},
\rho_{order},
\rho_{page},
\rho_{set},
\rho_{member},
\rho_{null},
\rho_{case},
\rho_{window},
\rho_{rec},
\ldots
\}.
$$

mutation builder 注册了 15 个 required operator family 和 7 个 supplementary operator family，共 22 个 mutation family。

## 6.2 核心 mutation 规则

### 比较与逻辑

$$
\begin{aligned}
\rho_{cmp}:&
\quad >\leftrightarrow\geq,
\quad <\leftrightarrow\leq,
\quad =\rightarrow\neq,\\
\rho_{logic}:&
\quad AND\leftrightarrow OR,\\
\rho_{where}:&
\quad WHERE\ p\rightarrow\epsilon.
\end{aligned}
$$

若 WHERE 根谓词为 AND，代码还存在删除一个合取项的路径；v15 manifest 中实际观察到的具体结果主要是删除整个 WHERE。

### JOIN

$$
\begin{aligned}
\rho_{join\_type}:&
\quad INNER\rightarrow LEFT,\\
&\quad LEFT/RIGHT/FULL\rightarrow INNER,\\
\rho_{join\_key}:&
\quad R.a=S.b\rightarrow R.a=S.c.
\end{aligned}
$$

其中 $S.c$ 必须是 schema 或查询引用中可以找到的另一个合法列。该规则不表示 NATURAL JOIN、USING、LATERAL 或任意方言专属 JOIN 都具有同等支持。

### GROUP BY、HAVING 与聚合

$$
\begin{aligned}
\rho_{group}:&
\quad GROUP\ BY(e_1,\ldots,e_n)
\rightarrow
GROUP\ BY(e_1,\ldots,e_{n-1}),\quad n\geq2,\\
\rho_{having}:&
\quad HAVING\ p[n]
\rightarrow
HAVING\ p[n+1],\\
\rho_{minmax}:&
\quad MIN\leftrightarrow MAX,\\
\rho_{sumavg}:&
\quad SUM\leftrightarrow AVG,\\
\rho_{count}:&
\quad COUNT(*)\leftrightarrow COUNT(c),\\
\rho_{aggdistinct}:&
\quad AGG(DISTINCT\ e)\rightarrow AGG(e).
\end{aligned}
$$

当前 HAVING mutation 针对整数形式的数值字面量，不表示能够修改任意数值表达式或复杂 HAVING 条件。

### DISTINCT、ORDER BY、LIMIT/OFFSET

$$
\begin{aligned}
\rho_{distinct-}:&
\quad SELECT\ DISTINCT\rightarrow SELECT,\\
\rho_{distinct+}:&
\quad SELECT\rightarrow SELECT\ DISTINCT,\\
\rho_{order}:&
\quad ASC\leftrightarrow DESC,\\
\rho_{limit}:&
\quad LIMIT\ n\rightarrow LIMIT(n+1),\\
\rho_{offset}:&
\quad OFFSET\ n\rightarrow OFFSET(n+1),\\
\rho_{limit+}:&
\quad ORDER\ BY\ o
\rightarrow
ORDER\ BY\ o\ LIMIT\ 1.
\end{aligned}
$$

ORDER mutation 会翻转方向并规范化目标方向的默认 NULL 排序，不能表述为“保持原查询 NULL 排序位置不变”。LIMIT/OFFSET mutation 当前针对整数字面量；OFFSET 代码和单元测试路径存在，但 v15 manifest 没有观察到具体 `offset_changed` 样本。

### 集合操作

$$
\rho_{set}:
\quad UNION\leftrightarrow UNION\ ALL.
$$

代码能够识别部分 INTERSECT/EXCEPT 结构，并可删除已经存在的 ALL 修饰，但 INTERSECT ALL 和 EXCEPT ALL 的公开样本落入执行边界，不能作为 SQLite mutation 的统一 S4 证据。

### Membership 与 NULL

$$
\begin{aligned}
\rho_{member}:&
\quad IN\leftrightarrow NOT\ IN,\\
&\quad EXISTS\leftrightarrow NOT\ EXISTS,\\
&\quad x\ IN(Q)\rightarrow EXISTS(Q'),\\
\rho_{null}:&
\quad IS\ NULL\rightarrow IS\ NOT\ NULL,\\
&\quad IS\ NULL\rightarrow =NULL.
\end{aligned}
$$

其中 `IN subquery → EXISTS` 只有部分 bounded evidence，公开 production audit 中尚未观察到该具体分支的 PASS；复杂相关、多层相关和 ANY/ALL/SOME 不属于无条件端到端支持范围。

### CASE

$$
\begin{aligned}
\rho_{case-when}:&
\quad CASE(W_1,\ldots,W_n,E)
\rightarrow
CASE(W_1,\ldots,W_{n-1},E),\\
\rho_{case-else}:&
\quad CASE(W_1,\ldots,W_n,E)
\rightarrow
CASE(W_1,\ldots,W_n).
\end{aligned}
$$

当前 mutation 是删除 WHEN 或 ELSE 分支，不表示覆盖任意 CASE 表达式等价改写。

### 窗口函数

$$
\begin{aligned}
\rho_{window-partition}:&
\quad PARTITION\ BY(e_1,\ldots,e_n)
\rightarrow
PARTITION\ BY(e_1,\ldots,e_{n-1}),\\
\rho_{window-order}:&
\quad ORDER\ BY\ e\ ASC
\leftrightarrow
ORDER\ BY\ e\ DESC.
\end{aligned}
$$

当前 registry 主要针对窗口 partition/order，不包括窗口 frame、named window 和复杂 frame exclusion 规则的统一 mutation 闭环。

### 递归 CTE

$$
\begin{aligned}
\rho_{recursive-step}:&
\quad e+n\rightarrow e+(n+1),\\
\rho_{recursive-stop}:&
\quad p[n]\rightarrow p[n+1].
\end{aligned}
$$

当前代码通过 `WITH RECURSIVE` 中的整数加减节点或 WHERE 数值字面量识别候选位置。这是句法定向的局部改写，不构成对任意复杂递归图、SEARCH/CYCLE 或 vendor 递归扩展的支持证明。

## 6.3 Mutation 证据要求

上述规则定义的是候选 mutation 的生成能力。某条规则只有在具体实例上满足：

$$
\mathsf{Generated}
\land
\mathsf{WitnessFound}
\land
\mathsf{RepairVerified}
$$

时，才计入 S3。满足当前 obligation、validator、repair 和 attribution 审计条件后，才计入 S4-audit。

---

# 7. 当前实现范围及证据分级

## 7.1 已观察到生产链完成证据的类别

在公开 v15 抽样中，以下错误类型至少存在一个通过当前 production audit 门槛的样本：

- 比较严格性及等值变化；
- AND/OR；
- WHERE 删除；
- JOIN 类型和 JOIN key；
- 数值 HAVING 阈值；
- 结果 DISTINCT 添加/删除；
- ORDER BY 方向；
- 数值 LIMIT 修改和添加 `LIMIT 1`；
- UNION/UNION ALL；
- IN/NOT IN、EXISTS/NOT EXISTS 中的部分正反变化；
- IS NULL/IS NOT NULL 中的部分变化；
- CASE WHEN/ELSE 删除；
- 窗口 partition/order；
- 递归步进表达式；
- MIN/MAX；
- `COUNT(column) → COUNT(*)`。

这一结论只表示“公开抽样中至少观察到一个当前门槛下的链路完成实例”，不表示相应类别中的任意表达式都达到端到端支持。

## 7.2 已实现代码或部分 bounded 证据，但不能统一声明为 S4-audit

以下能力存在实现、单元测试或部分 Gold 反例，但 v15 production audit 没有相应具体分支 PASS，或语料没有实际触发：

- 删除 GROUP BY key；
- `COUNT(*) → COUNT(column)`；
- 删除聚合 DISTINCT；
- 部分 SUM/AVG；
- 递归终止条件修改；
- `IN subquery → EXISTS`；
- `IS NULL → = NULL`；
- OFFSET 数值修改；
- 投影列缺失；
- INTERSECT ALL/EXCEPT ALL；
- 部分 RIGHT/FULL JOIN；
- 复杂相关子查询。

其中：

- OFFSET 路径和单元测试存在，但 v15 manifest 没有实际 `offset_changed` 样本；
- v15 的递归终止 mutation 有 28 个 `UNDECIDED`、2 个 `ENGINE_GAP`，没有找到 `NOT_EQUIVALENT` witness；
- INTERSECT ALL/EXCEPT ALL 的公开样本落入 `ENGINE_GAP`；
- `IN subquery → EXISTS` 虽有少量 Gold 反例，但没有 production-chain PASS；
- 投影列缺失已有运行时 ASTDiff、witness 和修复路径测试，但当前离线 mutation builder 没有“删除投影列”这一 operator。

## 7.3 主要属于 S1 的高级结构

结构 IR 和高级结构测试可以表示或识别：

- grouping sets、rollup、cube；
- named window、window frame；
- recursive SEARCH/CYCLE；
- lateral；
- DISTINCT ON、QUALIFY；
- 部分方言分页和高级结构。

这些证据主要证明字段建模、解析或 ASTDiff，即 S1。部分结构可能存在选择性转写或 bounded 测试，但不能作为该特性整体达到 S2、S3 或 S4-audit 的依据。

## 7.4 明确排除的统一端到端扩展

定义：

$$
\begin{aligned}
E_{struct}=\{&
NATURAL\ JOIN,
USING,
LATERAL,\\
&ROLLUP,
CUBE,
GROUPING\ SETS,\\
&named\ window,
complex\ window\ frame,\\
&SEARCH,
CYCLE,\\
&ANY,
ALL,
SOME,\\
&DISTINCT\ ON,
QUALIFY,\\
&TOP,
FETCH,
WITH\ TIES,\\
&INTERSECT\ ALL,
EXCEPT\ ALL
\}.
\end{aligned}
$$

若查询包含这些扩展，应采用：

$$
q\text{ contains }E_{struct}
\Rightarrow
\text{only feature-specific S1/S2 evidence may be claimed}.
$$

这不意味着这些结构全部不可用，而是它们不属于本文统一声明的端到端核心语言，其支持等级必须按具体特性和具体证据单独判定。

---

# 8. 实证结果

## 8.1 v15 mutation layer

公开 v15 mutation layer 读取 52,484 个唯一 train/public 查询家族，其中：

- 52,452 个成功生成 mutation；
- 52,452 个成功生成 equivalence control；
- 32 个属于解析缺口；
- 家族级生成覆盖率为 99.939029%；
- 15/15 个 required operator family 均至少被分配到一个样本；
- 22/22 个已注册 operator family 均具有非零样本。

这里的“15/15 覆盖”表示必需 family 均具有可分配样本，不表示：

- 只有 15 种 mutation；
- 每个 family 的全部具体分支都被语料触发；
- 每个样本都能找到 witness；
- 每个 family 都达到 S3 或 S4-audit。

## 8.2 公开 Gold Oracle 审计

v15 Gold Oracle 对 8,801 个分层抽样查询对进行了检验：

| Verdict | 数量 | 比例 |
|---|---:|---:|
| EQUIVALENT | 3,962 | 45.02% |
| NOT_EQUIVALENT | 3,665 | 41.64% |
| UNDECIDED | 755 | 8.58% |
| ENGINE_GAP | 419 | 4.76% |

在 4,801 个预期不等价的 mutation 样本中：

- 3,665 个找到反例，按全部 mutation 样本计算为 76.34%；
- 排除 ENGINE_GAP 后，反例发现率为 82.92%；
- 755 个仍为 UNDECIDED；
- 381 个为 ENGINE_GAP。

在 4,000 个等价控制中：

- 3,962 个得到受界 `EQUIVALENT`；
- 38 个为 `ENGINE_GAP`；
- 该公开抽样未观察到确定性控制反例。

报告中的条件式 100% 不能写成总体 mutation 检出率。由于样本采用按类别分层抽样，而不是从已知概率分布中独立同分布抽取，上述比例属于实验描述统计，不能直接解释为未知总体上的无偏成功率或置信保证。

## 8.3 公开 production-chain 审计

production-chain audit 共选择 435 个家族：

- PASS：318；
- EXCLUDED：117；
- FAIL：0。

其中预期不等价部分共有 353 个家族：

- 239 个达到当前端到端审计门槛；
- 114 个因为 Oracle 不确定、执行器缺口或已知生产边界而被排除；
- 相对于全部所选不等价样本，观察到的链路完成比例为：

$$
\frac{239}{353}=67.71\%.
$$

报告中的 `full_chain=239/239=100%` 只表示已经进入 PASS 集合的成功样本满足内部审计条件，不能解释为全部抽样家族的端到端支持率为 100%。

## 8.4 v16 hidden freeze

v16 hidden 实验包含 5,846 个家族：

- 5,839 个进入 mutation/control 配对范围；
- scope coverage 为 99.880260%；
- 两轮运行的汇总统计保持一致；
- 观察到 22 个确定性标签不匹配；
- 最终结果为 `acceptance.pass=false`。

按照当前 Oracle 的判定规则，预期不等价样本在所有有限世界一致时只会得到 `UNDECIDED`，不会得到 `EQUIVALENT`。因此，这 22 个 determinate mismatch 对应等价控制在 bounded world 中被发现不等价，说明 equivalence-control tactic 在 hidden 上并非全部保持其预期语义。

该结果说明 mutation 生成范围具有较高覆盖率和汇总层面的重复性，但不能声明 hidden 验收已经通过。

## 8.5 能力矩阵

公开 capability matrix 包含 52,452 个唯一开发家族，12 个核心类别均至少观察到 300 个家族。需要注意：

- 类别之间允许重叠，不是互斥划分；
- 类别计数不证明所有特性组合都存在；
- observed scenario axis 只表示语料中观察到相应特征；
- capability matrix 明确记录 `hidden_partition_read=false`；
- 结构类别覆盖不能直接解释为 S3 或 S4-audit 覆盖。

## 8.6 当前可复现测试

在 8GB 虚拟内存限制下重新运行明确列出的 Phase 1 相关测试，得到：

- Phase 1 核心、Oracle、mutation、audit、witness 和 attribution 套件：`425 passed, 24 skipped`；
- parseval、advanced structure 和 dialect semantics 补充套件：`427 passed`；
- 合计：`852 passed, 24 skipped`。

24 个 skip 全部来自 MySQL、PostgreSQL、T-SQL 和 Oracle 的 live native engine 当前不可用。因此，当前环境证明了 adapter 定义及非现场逻辑，但没有证明四种原生数据库在此次运行中均可连通执行。

历史声明中的 `354 passed, 12 skipped` 缺少对应的命令、测试清单或日志产物，不宜继续作为当前可复现测试证据。

---

# 9. 有效性威胁

## 9.1 内部有效性

- 当前端到端审计只要求至少一个 repair diff 和至少一个 attribution 知识点交集，弱于逐 atomic-diff 闭环；
- 有序结果识别部分依赖 SQL 文本中的 `ORDER BY`，可能受到嵌套 `ORDER BY` 影响；
- 个别 mutation 是句法定向变化，不保证在所有上下文中代表唯一语义错误；
- ORDER mutation 不保证保持原查询的 NULL 相对位置语义；
- hidden 冻结发生在 dirty working tree 上，部分未跟踪代码内容没有被 Git diff 完整绑定。

## 9.2 构造有效性

- mutation 是教学错误的可计算近似，不等价于真实学生错误分布；
- “找到反例”可以证明具体不等价，但“未找到反例”不能证明等价；
- knowledge-point overlap 只说明归因方向与差异存在交集，不能自动证明自然语言解释完全正确；
- 一个 operator family 获得样本并不证明其所有具体 mutation 分支都可达；
- equivalence control 是实验控制策略，其正确性仍需受界反例检验和 hidden 验证。

## 9.3 外部有效性

- 实验结果适用于所评估语料、schema 分布、mutation 策略和资源配置；
- 结果不能外推到任意 SQL、任意 schema、任意方言或任意特性组合；
- SQLite-compatible 执行不能替代其他数据库的原生语义；
- native engine 不可用时，系统只能报告能力缺口；
- 当前有限数据生成策略不能覆盖任意数据分布、约束组合和数据库统计特征。

## 9.4 结论有效性

- 公开实验是分层抽样实验，统计比例主要用于描述观察结果；
- 未建立总体概率分布时，不宜把实验比例解释为全体 SQL 上的成功概率；
- `EXCLUDED` 不能与 `PASS` 合并，也不能解释为系统判断正确；
- `FAIL=0` 必须与 117 个 EXCLUDED 同时报告；
- hidden 的 22 个确定性 label mismatch 必须保留，不能只报告 99.880260% scope coverage。

---

# 10. 实现与证据的可追溯位置

主要实现和结果产物包括：

- mutation registry 与重写实现：[`data_construct_test/scripts/build_phase1_mutation_layer.py`](data_construct_test/scripts/build_phase1_mutation_layer.py)；
- Gold Oracle：[`data_construct_test/scripts/phase1_gold_oracle.py`](data_construct_test/scripts/phase1_gold_oracle.py)；
- production-chain audit：[`data_construct_test/scripts/run_phase1_production_chain_audit.py`](data_construct_test/scripts/run_phase1_production_chain_audit.py)；
- SQLStructureIR：[`sql-edu-backend/core/ast_schema.py`](sql-edu-backend/core/ast_schema.py)；
- v15 mutation manifest：[`data_construct_test/outputs/phase1_corpus_universe_dev_v8/mutation_layer_manifest_v15.json`](data_construct_test/outputs/phase1_corpus_universe_dev_v8/mutation_layer_manifest_v15.json)；
- v15 Gold summary：[`data_construct_test/outputs/phase1_corpus_universe_dev_v8/gold_oracle_audit_stratified_10seed_v15_mysql_summary.json`](data_construct_test/outputs/phase1_corpus_universe_dev_v8/gold_oracle_audit_stratified_10seed_v15_mysql_summary.json)；
- v15 production summary：[`data_construct_test/outputs/phase1_corpus_universe_dev_v8/production_chain_audit_stratified_10seed_v15_mysql_per50_summary.json`](data_construct_test/outputs/phase1_corpus_universe_dev_v8/production_chain_audit_stratified_10seed_v15_mysql_per50_summary.json)；
- capability matrix：[`data_construct_test/outputs/phase1_corpus_universe_dev_v8/capability_matrix_train_public_observed_10seed_v15_full.json`](data_construct_test/outputs/phase1_corpus_universe_dev_v8/capability_matrix_train_public_observed_10seed_v15_full.json)；
- v16 hidden freeze：[`data_construct_test/outputs/phase1_corpus_universe_dev_v8/final_hidden_freeze_verification_v16.json`](data_construct_test/outputs/phase1_corpus_universe_dev_v8/final_hidden_freeze_verification_v16.json)。

---

# 11. 可直接用于论文正文的能力声明

> 本文实现了一套面向参考 SQL 与待分析 SQL 的分层差异诊断系统。系统能够将 SQL 解析为结构化中间表示，提取原子级 AST 差异，将差异转换为语义验证义务，并通过有限数据库构造、执行结果比较、候选修复验证和知识点归因形成样本级诊断证据。本文区分结构支持、受界执行支持、变异—修复支持和端到端链路完成证据，不将语法可解析性等同于完整诊断能力。
>
> 当前 mutation builder 注册了 15 个必需及 7 个补充 operator family。公开 v15 语料中，52,484 个唯一 train/public 查询家族有 52,452 个成功生成 mutation 和 equivalence control，家族级生成覆盖率为 99.939029%，parse gap 为 32。该统计证明 mutation/control 的可生成范围，不表示全部家族已经达到变异—修复支持或端到端诊断支持。
>
> v15 Gold Oracle 使用 seed 0..9、row scale 4/8/16、四类有限 world、单表最多 32 行、结果最多 256 行以及 SQLite 250,000 VM 步限制，对 8,801 个分层抽样查询对进行受界检验。实验发现 3,665 个不等价反例，3,962 个可信等价控制在有限世界内保持一致，另有 755 个 UNDECIDED 和 419 个 ENGINE_GAP。在 4,801 个预期不等价的 mutation 样本中，总体反例发现率为 76.34%，排除 ENGINE_GAP 后为 82.92%。这些结果属于给定生成策略和资源配置下的实验观察，不构成普遍查询等价证明。
>
> 在所选 353 个预期不等价的 production-chain audit 家族中，239 个达到当前端到端审计门槛，114 个因为 Oracle 不确定、执行器缺口或已知生产边界被排除，相对于全部所选不等价样本的链路完成比例为 67.71%。当前审计要求所有 atomic diff 均绑定 obligation 和 validator，同时至少一个 diff 获得 repair 证据且 attribution 与目标差异存在知识点交集；该条件尚弱于每个 atomic diff 的严格端到端 ID 闭环，因此本文将其称为 S4-audit，而不是无条件的完整 S4。
>
> 已经在公开抽样中观察到 S4-audit PASS 的能力包括比较边界、等值变化、布尔连接、WHERE 删除、JOIN 类型和 key、数值 HAVING、结果 DISTINCT、ORDER 方向、数值 LIMIT、UNION/UNION ALL、部分 IN/EXISTS 与 NULL 谓词变化、CASE 分支、窗口 partition/order、递归步进表达式，以及部分常见聚合。GROUP BY key 删除、COUNT(*) 到 COUNT(column)、聚合 DISTINCT、部分 SUM/AVG、递归终止条件、IN 子查询到 EXISTS、IS NULL 到 `= NULL`、OFFSET、投影列删除以及 INTERSECT/EXCEPT ALL 等能力目前只有代码、单元测试、部分受界证据或边界结果，不能统一声明为 S4-audit。
>
> generic、standard 和 sqlite 查询通过 SQLite-compatible 路径执行。MySQL、PostgreSQL、T-SQL 和 Oracle 已实现可选 native adapter，但只有在原生执行器已经配置且可达时才能产生相应方言的执行证据；不可用时系统返回 ENGINE_GAP，不静默降级到 SQLite。窗口 frame、named window、ROLLUP、CUBE、GROUPING SETS、SEARCH、CYCLE、LATERAL 及部分 vendor 分页结构已有不同程度的结构表示或专项测试，但结构支持不自动构成受界执行或端到端诊断支持。
>
> v16 hidden 实验对 5,846 个家族中的 5,839 个完成 mutation/control 配对，scope coverage 为 99.880260%，但仍存在 22 个确定性标签不匹配，因此最终验收结果为 `acceptance.pass=false`。上述结果说明系统已经对评估语料中的多类常见教学 SQL 错误建立了可执行、可重放的诊断证据，但不覆盖任意 SQL、任意 schema、任意方言或任意特性组合，也不构成对任意数据库实例上查询等价性的形式化保证。

---

# 12. 适合摘要或结论的精简版本

> Phase 1 面向共享 schema 和声明方言下的参考 SQL/学生 SQL 查询对，提供从结构差异提取、语义义务构造、有限 witness 生成、执行比较、修复验证到知识点归因的分层分析。系统能力按照具体查询对和资源配置划分为结构支持、受界执行支持、变异—修复支持及样本级端到端链路完成证据，不将可解析性或有限世界一致解释为普遍等价。
>
> 在公开 v15 语料上，系统对 52,484 个唯一查询家族中的 52,452 个成功生成 mutation 和等价控制，生成覆盖率为 99.939029%。Gold Oracle 对 8,801 个分层抽样查询对进行检验，发现 3,665 个不等价反例、3,962 个有限世界一致的可信等价控制、755 个 UNDECIDED 和 419 个 ENGINE_GAP。在 353 个预期不等价的生产链审计家族中，239 个达到当前 S4-audit 门槛，链路完成比例为 67.71%。hidden 实验的范围覆盖率为 99.880260%，但存在 22 个确定性标签不匹配，因此 hidden acceptance 尚未通过。
>
> 这些结果表明系统已对评估语料中的多类常见教学 SQL 错误建立了可执行和可重放的诊断证据，但该结论受 SQL 语法片段、schema 可重放性、方言执行器、有限数据生成策略和资源上限约束，不构成任意 SQL 或任意数据库上的形式化等价保证。
