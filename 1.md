结论：目前仓库没有 Dockerfile、Compose
  或方言执行器，SQLite 就是造数判题的唯一执行引擎。

  更严重的是，/home/roge/projects/sql-edu-main/sql-edu-backend/routers/ai.py:230 明确把 ParSEval 作为唯一判题来源；
  SQLite 标准答案执行失败后，会在 /home/roge/projects/sql-edu-main/sql-edu-backend/routers/ai.py:328 被当成
  is_correct=False。这会把平台能力不足误判成学生答错。

  建议方案

  先把系统收敛为 MySQL 方言，再逐步扩展。当前后端、题目生成和语法检查本来就偏 MySQL，这是成本最低且最可靠的路线。

  1. 立即止损

  - 判题结果改成 CORRECT / WRONG / UNSUPPORTED / ENGINE_ERROR / TIMEOUT，禁止将 standard_sql_failed 判成学生错误。
  - 建立 SQLite 能力矩阵。检测到 LATERAL、ROLLUP、CUBE、INTERSECT ALL、EXCEPT ALL、PIVOT 时直接返回 UNSUPPORTED。
  - 暂时下架超出能力矩阵的题目。
  - UDF 只能作为近似执行，不能作为最终判题依据。当前 ST_WITHIN 永远返回 1 等占位实现会产生静默误判，见 /home/roge/
    projects/sql-edu-main/sql-edu-backend/core/parseval_data_generator.py:4478。

  2. 把方言变成题目的强制属性

  当前题目模型只有 correct_sql，没有方言字段，见 /home/roge/projects/sql-edu-main/sql-edu-backend/models/
  question.py:21。建议增加：

  sql_dialect: mysql | postgres | tsql | sqlite
  engine_version: 可选

  所有解析、AST Diff 和执行都必须使用这个方言，不能再靠 _dialect_candidates() 猜测。sqlglot 保留用于解析和结构分析，但不
  再通过转译后的 SQLite 结果做最终裁决。

  3. 增加原生执行器

  推荐链路：

  题目方言 + 类型化 Schema
          -> 结构造数 Dataset IR
          -> 对应方言执行器
          -> 原生数据库执行标准/学生 SQL
          -> 结果集比较

  先实现 MySQLExecutor，之后再按真实题库需求增加 PostgresExecutor、TSQLExecutor。不要每次请求临时启动容器，使用预热的数
  据库容器池，每次判题创建独立临时 database/schema，执行完清理。

  Docker 数据库必须与业务主库完全隔离，并配置只读查询账号、查询超时、行数限制、CPU/内存限制和独立网络。Docker 本身并不自
  动等于安全沙箱。

  4. 补齐 Schema 类型

  现在 schema_preview 只有列名和样例行，没有类型、可空性和约束。即使换成 MySQL，错误的列类型仍会导致日期比较、整数除法、
  排序和 NULL 行为失真。应改成类似：

  {
    "name": "orders",
    "columns": [
      {"name": "id", "type": "BIGINT", "nullable": false},
      {"name": "created_at", "type": "DATETIME", "nullable": false}
    ]
  }

  5. 迁移方式

  - 第一阶段：MySQL 原生执行器以 shadow 模式运行，同时记录 SQLite 与 MySQL 的结果差异。
  - 第二阶段：MySQL 题目切换到原生执行器，SQLite 仅保留给明确标注为 SQLite/ANSI 子集的测试。
  - 第三阶段：根据题库统计决定是否值得部署 PostgreSQL、SQL Server。
  - DuckDB 可以作为本地分析型 fallback，但不能代表 MySQL 或 SQL Server 的真实语义；单纯把 SQLite 换成 DuckDB/PostgreSQL
    解决不了多方言问题。

  现有报告已经列出明确边界：606 个用例中有 20 个已知边界，失败集中在 CUBE、ROLLUP、INTERSECT ALL、EXCEPT ALL、LATERAL，
  见 /home/roge/projects/sql-edu-main/data_construct_test/outputs/phase1_cfg_convergence_report.md:10。所以正确方向不是
  继续无限补 SQLite，而是“显式方言 + 类型化造数 + 原生执行器 + 三态/多态判题结果”。